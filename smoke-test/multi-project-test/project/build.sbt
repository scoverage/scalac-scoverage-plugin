resolvers += Classpaths.typesafeResolver

addSbtPlugin("ch.craven" % "scct-plugin" % "0.2.1")

// addSbtPlugin("org.example" %% "example-plugin" % "0.1-SNAPSHOT")

// doesn't work: libraryDependencies += "ch.craven" %% "scct-plugin" % "0.2.1"


